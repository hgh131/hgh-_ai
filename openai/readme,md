#OPENAI AIGC 开创者

-调用openai llm接口
-命令行调用(js做后端运行)
  -npm init -y  初始化为node 后端项目
     npm？ node package management 包管理
  -package.json 项目描述文件
  -npm install openai 安装openai包
     将openai sdk下载到本地
     ndoe_modules依赖的模块，openai本身也依赖的模块

-openai 提供了sdk